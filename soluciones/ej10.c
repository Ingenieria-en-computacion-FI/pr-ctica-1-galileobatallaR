#include <stdio.h>
int gigante[10000000];

int main(){
    // Resolver ejercicio 10
    gigante[0] = 1
    printf("declarar la matriz como global hace que se almacene en segmento de datos, no en el stack y permite tamaños mayores de memoria.\n"); 
    return 0;
}
